# LiquidityOS MVP 上线记录

**日期**：2026-03-11  
**状态**：已上线  
**阶段**：独立站 MVP

## 1. 上线结论

LiquidityOS 已完成从 Beta 工具版到网页独立站 MVP 的迁移，并已成功上线。

当前线上形态为：

- 前端：Cloudflare Pages
- 宏观数据接口：Cloudflare Worker

当前已确认：MVP 核心主链路在生产环境可用。

## 2. 线上地址

### 前端地址

- [https://093499c4.liquidity-os-mvp.pages.dev](https://093499c4.liquidity-os-mvp.pages.dev)

### Worker 地址

- [https://liquidityos-data.fanfan09132022.workers.dev](https://liquidityos-data.fanfan09132022.workers.dev)

## 3. 本轮上线范围

本轮上线目标不是扩展功能，而是将已通过 Beta 验收的核心闭环稳定迁移到独立站环境。

已上线范围包括：

- L0-A 周期偏见选择
- L1 自动 / 手动双模式
- L2 / L3 / F&G / L4 / Hero 统一信号逻辑
- 每日快照保存
- 按日期回看
- 近 30 天历史摘要
- 缓存优先读取
- 强制刷新
- 系统状态观测层
- dirty-save 保存边界

## 4. 前端迁移说明

前端采用最小迁移策略完成独立站承接：

- 使用 Vite + React 构建独立站壳
- 原单文件 dashboard 主逻辑整体迁入 `src/App.jsx`
- 使用 `localStorage` 承接原有 `window.storage` 持久化协议
- Worker 请求收口到前端配置项 `VITE_WORKER_BASE_URL`
- 未在本轮迁移中重写业务逻辑、状态结构或评分公式

## 5. Worker 部署说明

Worker 继续承担宏观数据聚合职责，保持现有 API 结构不变。

本轮部署前补充了最小跨域支持：

- 动态生成 CORS headers
- 统一处理 `OPTIONS`
- 支持前端跨域访问 Worker
- 增加 `Vary: Origin`

本轮未改动宏观数据业务逻辑和接口结构。

## 6. Pages 配置

本轮前端部署采用本地构建后上传方式。

已使用配置：

- Framework preset: `Vite`
- Build command: `npm run build`
- Build output directory: `dist`

前端环境变量：

- `VITE_WORKER_BASE_URL=https://liquidityos-data.fanfan09132022.workers.dev`

说明：

- 本次线上 bundle 使用的是本地构建时注入的环境变量
- Pages 项目中也已保存同名变量
- 如果后续切换为 Git 自动构建，需要确保 Pages 构建环境中同步配置该变量

## 7. 生产环境验证结果

本轮上线后已完成以下 3 条核心线上链路验证：

### 7.1 强制刷新 → Worker 返回 → 页面写入当天快照

结果：通过

验证现象：

- 页面显示 `08:48 更新 · Worker`
- 系统状态显示 `缓存读取层：网络成功`
- 系统状态显示 `快照保存层：已保存`
- 浏览器 `localStorage` 中写入：
  - `daily:2026-03-11`
  - `macroMeta = { time: "08:48", source: "Worker" }`

### 7.2 读取缓存 → 命中当天快照 → 页面回填

结果：通过

验证现象：

- 页面顶部来源变为 `Worker · 缓存`
- 系统状态显示 `缓存读取层：缓存命中`
- 页面提示 `2026-03-11 宏观快照已回填 · 08:48`

### 7.3 切到昨天 → 历史只读 → 切回今天恢复今日数据

结果：通过

验证现象：

- 切到昨天后页面显示：
  - `历史只读`
  - `历史日期不请求网络，只读本地快照`
  - `只读空白`
- 切回今天后：
  - 今日快照恢复
  - 再次读取缓存仍可命中当天快照

## 8. 当前确认成立的线上边界

经本轮生产环境验证，当前版本已确认满足以下边界：

- 强制刷新可直连 Worker 并写入当日快照
- 读取缓存优先回填本地快照
- 历史日期只读，不触发网络请求
- 历史查看不会误触发保存
- 缓存回填不会误触发保存
- dirty-save 机制在线上环境保持成立
- 历史回看与今日工作区不会串台

## 9. 当前非阻塞问题

以下问题当前不影响 MVP 使用，但应在后续迭代中处理：

- `favicon.ico` 404
- 当前 Pages 为“本地构建后上传”，尚未切换到 Git 自动构建链路
- Pages 环境变量虽已配置，但后续如改用自动构建，需再次确认构建环境注入是否一致

## 10. 阶段性结论

截至本次上线，LiquidityOS 已完成从内部工具到网页独立站 MVP 的阶段转换。

当前项目状态可表述为：

> LiquidityOS 独立站 MVP 已完成上线，Beta 核心闭环已在生产环境验证通过。

## 11. 后续建议

下一阶段建议继续保持小步迭代，不立即扩展大功能。  
优先级更高的事项包括：

- 补 favicon
- 增加 `robots` / `noindex` 策略
- 切换为 Git 自动构建部署
- README 同步线上地址与当前状态
- 继续细化错误分级与缓存策略
